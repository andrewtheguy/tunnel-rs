[workspace]
resolver = "2"
members = [
    "crates/tunnel-rs",
]


[workspace.package]
version = "0.2.4"

[workspace.dependencies]
iroh = { version = "0.96.1", features = ["address-lookup-mdns"] }

# [profile.release]
# strip = true
# lto = "fat"
# codegen-units = 1
# panic = "abort"
